---
description: MVT Events (backend) — commit + push (./scripts/git-push.sh)
---

No terminal, na raiz de **mvt-events**:

```bash
./scripts/git-push.sh
```

Ao executar pela ferramenta do agente: use permissões **somente** `network` e `git_write` (evite `all` — isso reduz prompts extras). Mostre a saída completa.

Mensagem PT-BR automática (heurística ou IA). Só heurística: `GIT_COMMIT_AI=0 ./scripts/git-push.sh`.
