---
description: MVT Events (backend) — commit + push (./scripts/git-push.sh)
---

No terminal, na raiz de **mvt-events**:

```bash
./scripts/git-push.sh
```

Permissões de rede/git se necessário; mostrar a saída. Mensagem PT-BR automática (heurística ou IA). Só heurística: `GIT_COMMIT_AI=0 ./scripts/git-push.sh`.
